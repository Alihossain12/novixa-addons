/**
 * Novixa Addons — Elementor editor-panel behaviour.
 * Reserved for future panel enhancements.
 */
( function ( $ ) {
	'use strict';

	// Intentionally minimal for the initial widget set (Heading, Text Editor).

} )( jQuery );
